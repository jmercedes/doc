<h1>Listing doctor_schedules</h1>

<table>
  <tr>
    <th>Doctor</th>
    <th>Schedule</th>
    <th></th>
    <th></th>
    <th></th>
  </tr>

<% @doctor_schedules.each do |doctor_schedule| %>
  <tr>
    <td><%= doctor_schedule.doctor_id %></td>
    <td><%= doctor_schedule.schedule_id %></td>
    <td><%= link_to 'Show', doctor_schedule %></td>
    <td><%= link_to 'Edit', edit_doctor_schedule_path(doctor_schedule) %></td>
    <td><%= link_to 'Destroy', doctor_schedule, method: :delete, data: { confirm: 'Are you sure?' } %></td>
  </tr>
<% end %>
</table>

<br />

<%= link_to 'New Doctor schedule', new_doctor_schedule_path %>
